/**
 * Phases Lunaires Réelles 2026
 * Sources: lunaf.com, timeanddate.com, online-siesta.com
 * Données astronomiques précises pour l'année 2026
 * Inclut les 4 phases principales : Nouvelle Lune, Premier Quartier, Pleine Lune, Dernier Quartier
 */

export const moonPhases2026 = [
  // JANVIER 2026
  { date: '2026-01-03', time: '11:02', phase: 'Pleine Lune', emoji: '🌕' },
  { date: '2026-01-10', time: '15:48', phase: 'Dernier Quartier', emoji: '🌗' },
  { date: '2026-01-18', time: '20:52', phase: 'Nouvelle Lune', emoji: '🌑' },
  { date: '2026-01-26', time: '04:47', phase: 'Premier Quartier', emoji: '🌓' },

  // FÉVRIER 2026
  { date: '2026-02-01', time: '23:09', phase: 'Pleine Lune', emoji: '🌕' },
  { date: '2026-02-09', time: '12:43', phase: 'Dernier Quartier', emoji: '🌗' },
  { date: '2026-02-17', time: '13:01', phase: 'Nouvelle Lune', emoji: '🌑' },
  { date: '2026-02-24', time: '12:28', phase: 'Premier Quartier', emoji: '🌓' },

  // MARS 2026
  { date: '2026-03-03', time: '12:37', phase: 'Pleine Lune', emoji: '🌕' },
  { date: '2026-03-11', time: '09:39', phase: 'Dernier Quartier', emoji: '🌗' },
  { date: '2026-03-19', time: '02:23', phase: 'Nouvelle Lune', emoji: '🌑' },
  { date: '2026-03-25', time: '19:18', phase: 'Premier Quartier', emoji: '🌓' },

  // AVRIL 2026
  { date: '2026-04-02', time: '04:11', phase: 'Pleine Lune', emoji: '🌕' },
  { date: '2026-04-10', time: '04:52', phase: 'Dernier Quartier', emoji: '🌗' },
  { date: '2026-04-17', time: '13:51', phase: 'Nouvelle Lune', emoji: '🌑' },
  { date: '2026-04-24', time: '02:32', phase: 'Premier Quartier', emoji: '🌓' },

  // MAI 2026
  { date: '2026-05-01', time: '19:23', phase: 'Pleine Lune', emoji: '🌕' },
  { date: '2026-05-09', time: '21:10', phase: 'Dernier Quartier', emoji: '🌗' },
  { date: '2026-05-16', time: '22:01', phase: 'Nouvelle Lune', emoji: '🌑' },
  { date: '2026-05-23', time: '11:12', phase: 'Premier Quartier', emoji: '🌓' },
  { date: '2026-05-31', time: '10:45', phase: 'Pleine Lune', emoji: '🌕' },

  // JUIN 2026
  { date: '2026-06-08', time: '10:03', phase: 'Dernier Quartier', emoji: '🌗' },
  { date: '2026-06-15', time: '04:54', phase: 'Nouvelle Lune', emoji: '🌑' },
  { date: '2026-06-21', time: '21:55', phase: 'Premier Quartier', emoji: '🌓' },
  { date: '2026-06-30', time: '01:56', phase: 'Pleine Lune', emoji: '🌕' },

  // JUILLET 2026
  { date: '2026-07-07', time: '19:30', phase: 'Dernier Quartier', emoji: '🌗' },
  { date: '2026-07-14', time: '11:43', phase: 'Nouvelle Lune', emoji: '🌑' },
  { date: '2026-07-21', time: '11:05', phase: 'Premier Quartier', emoji: '🌓' },
  { date: '2026-07-29', time: '16:35', phase: 'Pleine Lune', emoji: '🌕' },

  // AOÛT 2026
  { date: '2026-08-06', time: '02:22', phase: 'Dernier Quartier', emoji: '🌗' },
  { date: '2026-08-12', time: '19:36', phase: 'Nouvelle Lune', emoji: '🌑' },
  { date: '2026-08-20', time: '02:46', phase: 'Premier Quartier', emoji: '🌓' },
  { date: '2026-08-28', time: '06:18', phase: 'Pleine Lune', emoji: '🌕' },

  // SEPTEMBRE 2026
  { date: '2026-09-04', time: '07:52', phase: 'Dernier Quartier', emoji: '🌗' },
  { date: '2026-09-11', time: '05:27', phase: 'Nouvelle Lune', emoji: '🌑' },
  { date: '2026-09-18', time: '20:44', phase: 'Premier Quartier', emoji: '🌓' },
  { date: '2026-09-26', time: '18:49', phase: 'Pleine Lune', emoji: '🌕' },

  // OCTOBRE 2026
  { date: '2026-10-03', time: '13:26', phase: 'Dernier Quartier', emoji: '🌗' },
  { date: '2026-10-10', time: '17:50', phase: 'Nouvelle Lune', emoji: '🌑' },
  { date: '2026-10-18', time: '16:13', phase: 'Premier Quartier', emoji: '🌓' },
  { date: '2026-10-26', time: '05:11', phase: 'Pleine Lune', emoji: '🌕' },

  // NOVEMBRE 2026
  { date: '2026-11-01', time: '20:30', phase: 'Dernier Quartier', emoji: '🌗' },
  { date: '2026-11-09', time: '08:02', phase: 'Nouvelle Lune', emoji: '🌑' },
  { date: '2026-11-17', time: '11:48', phase: 'Premier Quartier', emoji: '🌓' },
  { date: '2026-11-24', time: '15:53', phase: 'Pleine Lune', emoji: '🌕' },

  // DÉCEMBRE 2026
  { date: '2026-12-01', time: '06:10', phase: 'Dernier Quartier', emoji: '🌗' },
  { date: '2026-12-09', time: '01:51', phase: 'Nouvelle Lune', emoji: '🌑' },
  { date: '2026-12-16', time: '21:42', phase: 'Premier Quartier', emoji: '🌓' },
  { date: '2026-12-24', time: '02:28', phase: 'Pleine Lune', emoji: '🌕' }
];

/**
 * Description et illumination pour chaque phase principale
 */
const phaseDetails = {
  'Nouvelle Lune': { description: 'Nouveau départ, intentions', illumination: 0 },
  'Premier Quartier': { description: 'Construction, détermination', illumination: 50 },
  'Pleine Lune': { description: 'Accomplissement, gratitude', illumination: 100 },
  'Dernier Quartier': { description: 'Libération, pardon', illumination: 50 }
};

/**
 * Descriptions des phases intermédiaires
 */
const intermediatePhases = {
  'Premier Croissant': { emoji: '🌒', description: 'Germination, action' },
  'Gibbeuse Croissante': { emoji: '🌔', description: 'Raffinement, ajustement' },
  'Gibbeuse Décroissante': { emoji: '🌖', description: 'Partage, récolte' },
  'Dernier Croissant': { emoji: '🌘', description: 'Repos, introspection' }
};

/**
 * Détermine la phase intermédiaire entre deux phases principales consécutives
 */
const getIntermediatePhase = (prevPhase, nextPhase, progress) => {
  const transitions = {
    'Nouvelle Lune->Premier Quartier': {
      name: 'Premier Croissant',
      emoji: '🌒',
      description: 'Germination, action'
    },
    'Premier Quartier->Pleine Lune': {
      name: 'Gibbeuse Croissante',
      emoji: '🌔',
      description: 'Raffinement, ajustement'
    },
    'Pleine Lune->Dernier Quartier': {
      name: 'Gibbeuse Décroissante',
      emoji: '🌖',
      description: 'Partage, récolte'
    },
    'Dernier Quartier->Nouvelle Lune': {
      name: 'Dernier Croissant',
      emoji: '🌘',
      description: 'Repos, introspection'
    }
  };

  const key = `${prevPhase}->${nextPhase}`;
  return transitions[key] || null;
};

/**
 * Calcule l'illumination approximative en fonction de la position entre deux phases
 */
const calculateIllumination = (prevPhase, nextPhase, progress) => {
  const illumMap = {
    'Nouvelle Lune': 0,
    'Premier Quartier': 50,
    'Pleine Lune': 100,
    'Dernier Quartier': 50
  };

  const prevIllum = illumMap[prevPhase] ?? 50;
  const nextIllum = illumMap[nextPhase] ?? 50;

  // Gérer la transition Dernier Quartier -> Nouvelle Lune (50 -> 0)
  // et Nouvelle Lune -> Premier Quartier (0 -> 50)
  return Math.round(prevIllum + (nextIllum - prevIllum) * progress);
};

/**
 * Calcule la phase lunaire précise pour une date donnée
 * en se basant sur les données astronomiques réelles (4 phases principales)
 */
export const getAccurateMoonPhase = (date) => {
  const targetDate = new Date(date);
  targetDate.setHours(0, 0, 0, 0);

  // Trouver les phases lunaires avant et après la date cible
  let previousPhase = null;
  let nextPhase = null;

  for (let i = 0; i < moonPhases2026.length; i++) {
    const phaseDate = new Date(moonPhases2026[i].date);
    phaseDate.setHours(0, 0, 0, 0);

    if (phaseDate.getTime() === targetDate.getTime()) {
      // On est exactement sur une phase principale
      const details = phaseDetails[moonPhases2026[i].phase];
      return {
        name: moonPhases2026[i].phase,
        emoji: moonPhases2026[i].emoji,
        description: details ? details.description : '',
        illumination: details ? details.illumination : 50,
        exactTime: moonPhases2026[i].time
      };
    }

    if (phaseDate < targetDate) {
      previousPhase = moonPhases2026[i];
    }
    if (phaseDate > targetDate && !nextPhase) {
      nextPhase = moonPhases2026[i];
      break;
    }
  }

  if (!previousPhase || !nextPhase) {
    // Fallback sur le calcul algorithmique si hors période
    return null;
  }

  const prevDate = new Date(previousPhase.date);
  const nextDate = new Date(nextPhase.date);
  const daysSincePrev = Math.floor((targetDate - prevDate) / (1000 * 60 * 60 * 24));
  const totalDays = Math.floor((nextDate - prevDate) / (1000 * 60 * 60 * 24));
  const progress = daysSincePrev / totalDays;

  // Proche d'une phase principale (dans les 1 jour)
  if (progress < 0.15) {
    const details = phaseDetails[previousPhase.phase];
    return {
      name: previousPhase.phase,
      emoji: previousPhase.emoji,
      description: details ? details.description : '',
      illumination: details ? details.illumination : 50
    };
  }
  if (progress > 0.85) {
    const details = phaseDetails[nextPhase.phase];
    return {
      name: nextPhase.phase,
      emoji: nextPhase.emoji,
      description: details ? details.description : '',
      illumination: details ? details.illumination : 50
    };
  }

  // Phase intermédiaire
  const intermediate = getIntermediatePhase(previousPhase.phase, nextPhase.phase, progress);
  if (intermediate) {
    return {
      name: intermediate.name,
      emoji: intermediate.emoji,
      description: intermediate.description,
      illumination: calculateIllumination(previousPhase.phase, nextPhase.phase, progress)
    };
  }

  return null;
};

/**
 * Obtient la prochaine pleine lune
 */
export const getNextFullMoon = (fromDate = new Date()) => {
  const target = new Date(fromDate);
  target.setHours(0, 0, 0, 0);

  for (const phase of moonPhases2026) {
    if (phase.phase === 'Pleine Lune') {
      const phaseDate = new Date(phase.date);
      if (phaseDate >= target) {
        return {
          ...phase,
          daysUntil: Math.ceil((phaseDate - target) / (1000 * 60 * 60 * 24))
        };
      }
    }
  }
  return null;
};

/**
 * Obtient la prochaine nouvelle lune
 */
export const getNextNewMoon = (fromDate = new Date()) => {
  const target = new Date(fromDate);
  target.setHours(0, 0, 0, 0);

  for (const phase of moonPhases2026) {
    if (phase.phase === 'Nouvelle Lune') {
      const phaseDate = new Date(phase.date);
      if (phaseDate >= target) {
        return {
          ...phase,
          daysUntil: Math.ceil((phaseDate - target) / (1000 * 60 * 60 * 24))
        };
      }
    }
  }
  return null;
};

/**
 * Obtient le prochain premier quartier
 */
export const getNextFirstQuarter = (fromDate = new Date()) => {
  const target = new Date(fromDate);
  target.setHours(0, 0, 0, 0);

  for (const phase of moonPhases2026) {
    if (phase.phase === 'Premier Quartier') {
      const phaseDate = new Date(phase.date);
      if (phaseDate >= target) {
        return {
          ...phase,
          daysUntil: Math.ceil((phaseDate - target) / (1000 * 60 * 60 * 24))
        };
      }
    }
  }
  return null;
};

/**
 * Obtient le prochain dernier quartier
 */
export const getNextLastQuarter = (fromDate = new Date()) => {
  const target = new Date(fromDate);
  target.setHours(0, 0, 0, 0);

  for (const phase of moonPhases2026) {
    if (phase.phase === 'Dernier Quartier') {
      const phaseDate = new Date(phase.date);
      if (phaseDate >= target) {
        return {
          ...phase,
          daysUntil: Math.ceil((phaseDate - target) / (1000 * 60 * 60 * 24))
        };
      }
    }
  }
  return null;
};

/**
 * Obtient toutes les phases lunaires pour un mois donné
 */
export const getMoonPhasesForMonth = (year, month) => {
  return moonPhases2026.filter(phase => {
    const phaseDate = new Date(phase.date);
    return phaseDate.getFullYear() === year && phaseDate.getMonth() === month;
  });
};

/**
 * Vérifie si une date est une pleine lune
 */
export const isFullMoon = (date) => {
  const target = new Date(date);
  target.setHours(0, 0, 0, 0);

  return moonPhases2026.some(phase => {
    const phaseDate = new Date(phase.date);
    phaseDate.setHours(0, 0, 0, 0);
    return phase.phase === 'Pleine Lune' && phaseDate.getTime() === target.getTime();
  });
};

/**
 * Vérifie si une date est une nouvelle lune
 */
export const isNewMoon = (date) => {
  const target = new Date(date);
  target.setHours(0, 0, 0, 0);

  return moonPhases2026.some(phase => {
    const phaseDate = new Date(phase.date);
    phaseDate.setHours(0, 0, 0, 0);
    return phase.phase === 'Nouvelle Lune' && phaseDate.getTime() === target.getTime();
  });
};

export default moonPhases2026;
